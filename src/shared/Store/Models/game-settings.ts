export interface GameSettings {
  points_log: number;
  points_log_first_of_day: number;
  points_peer_assessment: number;
  points_peer_assessment_first_of_team: number;
  points_project_evaluation: number;
  points_project_evaluation_first_of_team: number;
  max_logs_per_day: number;
  points_project_evaluation_submitted_first_day: number;
  points_peer_assessment_submitted_first_day: number;
}
